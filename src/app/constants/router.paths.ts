enum paths {
    MAIN_PAGE = 'youtube',
    AUTH_PAGE = 'auth',
    DETAIL = 'detail',
    REGISTER = 'register',
    ADMIN = 'admin',
    NOT_FOUND = 'not-found',
    CUSTOM = 'custom'
}

export default paths;
